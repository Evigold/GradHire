CREATE TABLE SCHOOLS 
(
  ID INTEGER NOT NULL,
  SchName VARCHAR(32) NOT NULL,
  RANKING INTEGER NOT NULL,
  PRIMARY KEY (ID) 
);
INSERT INTO SCHOOLS(110, 'UW Tacoma', 1);
INSERT INTO SCHOOLS(110, 'UW Seattle', 2);

CREATE TABLE STUDENTS (
  ID INTEGER NOT NULL,
  SchoolID INTEGER NOT NULL,
  StuName VARCHAR(32) NOT NULL,
  GradYear INTEGER NOT NULL,
  PastExp INTEGER NOT NULL,
  PRIMARY KEY(ID),
  FOREIGN KEY (SchoolID) REFERENCES SCHOOLS (ID) 
)

CREATE TABLE COMPANY (
  ID INTEGER NOT NULL,
  CName VARCHAR(32) NOT NULL,
  RANKING INTEGER NOT NULL,
  PRIMARY KEY (ID) 
)

CREATE TABLE JOBS (
  ID INTEGER NOT NULL,
  CID INTEGER NOT NULL,
  JName VARCHAR(32) NOT NULL,
  YearReq INTEGER NOT NULL,
  PastExp INTEGER NOT NULL,
  Compensation REAL NOT NULL,
  PRIMARY KEY (ID),
  FOREIGN KEY (CID) REFERENCES COMPANY (ID) 
)
